# Core LangChain Libraries (Pinned for stability)
langchain>=0.2.0
langchain-core==0.2.14  # Pin a specific, stable version to fix conflicts
langchain-chroma>=0.1.2 # The module that failed to load
langchain-community     # Make sure it's here without pinning a new version 

# Chroma Vector Store Dependencies (Includes SQLite fix)
chromadb>=0.5.3
pysqlite3-binary        # Critical fix for Chroma on Streamlit Cloud
faiss-cpu

# LLM and Embedding Dependencies
sentence-transformers>=2.2.2
langchain-huggingface>=0.0.6
transformers>=4.33.0
torch>=2.0.0
accelerate>=0.21.0

# Data and Utilities
pandas>=2.1.0
tqdm>=4.65.0
pypdf
langchain-text-splitters
